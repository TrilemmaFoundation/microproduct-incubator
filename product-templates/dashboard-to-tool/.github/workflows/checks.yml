name: Checks stub

on:
  push:
    branches: ['main']
  pull_request:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo 'Attach lint/test toolchain per stack — see README.md'
